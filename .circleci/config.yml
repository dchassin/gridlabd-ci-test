version: 2
jobs:
  build:
    docker:
      - image: gridlabd/slac-master
    steps:
      - checkout            
      - run:
          name: Run the model
          command: |
            make all
      - run:
          name: Deliver results
          command: |
            cat my_test.csv